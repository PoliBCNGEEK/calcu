{
  "scripts": {
    "test": "cucumber-js --require cucumber.conf.js --require *.steps.js --tags \"not @Manual\" --parallel 1"
  },
  "dependencies": {
    "yarn": "^1.22.19"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.5.2",
    "@playwright/test": "^1.25.2",
    "playwright": "^1.25.2"
  }
}
